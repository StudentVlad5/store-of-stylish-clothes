export const selectBasket = state => state.basket;
export const selectTotalAmount = state => state.basket.totalAmount;
export const selectTotalDiscount = state => state.basket.totalDiscount;
export const selectTotalPayment = state => state.basket.totalPayment;
export const selectCurrency = state => state.basket.currency;
